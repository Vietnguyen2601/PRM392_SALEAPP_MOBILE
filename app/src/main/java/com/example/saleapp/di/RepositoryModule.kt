package com.example.saleapp.di

// RepositoryModule is intentionally empty.
// Repositories use @Singleton + @Inject constructor, so Hilt can provide them automatically
// without explicit @Provides methods.

